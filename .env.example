DATABASE_URL=postgresql://username:password@localhost:5433/landing_page
JWT_SECRET=f625de870845b575d0b8c40d200a83f8
CORS_ORIGINS=http://localhost:5173
PORT=9000

# Frontend variables (prefixed with VITE_ for Vite)
VITE_API_BASE_URL=http://localhost:9000/api
VITE_GEMINI_API_KEY=your-gemini-api-key
# Optional: override Vite base path for static hosting (defaults to "/")
# VITE_BASE_PATH=/

# Ollama settings (for the admin AI Content Studio)
# Point OLLAMA_HOST at wherever Ollama is running.
# - When running the backend in Docker against a host-local Ollama, keep host.docker.internal.
# - If Ollama is on another machine, use its LAN IP instead (e.g. http://192.168.1.40:11434).
OLLAMA_HOST=http://host.docker.internal:11434
OLLAMA_MODEL=mistral
# Optional comma-separated fallback hosts. Each entry should include protocol + port.
# Example: OLLAMA_HOST_FALLBACKS=http://host.docker.internal:11434,http://192.168.1.40:11434
# OLLAMA_HOST_FALLBACKS=
# Override the auto-detected Docker bridge hostname (defaults to host.docker.internal when OLLAMA_HOST points to localhost)
# OLLAMA_DOCKER_BRIDGE_HOST=host.docker.internal
